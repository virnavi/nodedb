/// Pre-compiled NodeDB native binaries for Flutter.
///
/// This package distributes the compiled `libnodedb_ffi` native library
/// for each supported platform. Add it as a dependency to ensure the
/// native library is bundled with your Flutter app.
library;
